modules

	waveform module

		 # make these 3 as accumulator objects ?
		.get mean waveform mea
		.get trace
		.get cut 

		filter module
			generator_of_filtered waveform (to plug with waveform stream)
			.notch
			.bandpass
			.arbitrary (im and re part)

		spectrum
			.mean power_spec  # also as accumulator object
			.get_freq_from_time

		fitting module
			.fit to reference(  )
